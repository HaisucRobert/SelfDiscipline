package info.androidhive.fingerprint;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class Meals_generator extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_meals_generator);


        // Here we need that random form db and display a list with food aproximatively equal with last value from Goals
        // Also here i need to add a dataGridView with all data from database
    }
}
